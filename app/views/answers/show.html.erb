<h1><%= @answer.answer%></h1>



<% if current_user != nil && current_user.id == @answer.user_id %>
  <p><%= link_to "Delete", answer_path(@answer), method: "delete", :class => 'btn btn-danger' %></p>
<% end %>
<p><%= link_to 'Home', question_path(@question)%>